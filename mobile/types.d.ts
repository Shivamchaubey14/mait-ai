/**
 * Ambient type references.
 *
 * React Native Testing Library registers its element matchers automatically from v12.4, and
 * ships their types with the main entry point — so nothing extra is needed here for those.
 * This file exists for ambient declarations the project adds later.
 */

export {};
