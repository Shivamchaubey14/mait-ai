# Pins are exact so every environment and every CI run resolves the same tree.
# Dependabot proposes bumps weekly; pip-audit gates them in CI (see SECURITY.md).

# Core framework
# 5.1+ is required for CheckConstraint(condition=); the old check= spelling is removed in
# Django 6. Staying on the 5.2 LTS line, which is what the SRS specifies.
Django==5.2.16
djangorestframework==3.17.2
django-environ==0.14.0
django-cors-headers==4.9.0
django-filter==26.1

# API schema / docs
drf-spectacular==0.30.0
drf-spectacular-sidecar==2026.8.1

# Auth
djangorestframework-simplejwt==5.5.1
PyJWT==2.13.0

# Database
mysqlclient==2.2.8

# Async jobs
celery==5.6.3
django-celery-beat==2.9.0
django-celery-results==2.6.0
redis==8.1.0

# Caching / throttling
django-redis==7.0.0

# Storage
django-storages==1.14.6
boto3==1.42.4
Pillow==12.3.0

# SAP xlsx ingest
openpyxl==3.1.5

# Outbound HTTP (Indent Easy connector, SMS gateway)
requests==2.34.2

# PII encryption at rest
cryptography==50.0.0

# Serving
gunicorn==26.0.0
whitenoise==6.12.0

# Observability
sentry-sdk==2.66.1
django-prometheus==2.4.1

# Utilities
python-dateutil==2.9.0.post0
phonenumbers==9.0.36
